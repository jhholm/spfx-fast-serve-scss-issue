import * as React from 'react';
import { DefaultButton } from 'office-ui-fabric-react/lib/Button';

const TestButton = (): JSX.Element => {
  return <DefaultButton>Test</DefaultButton>;
};

export default TestButton;
